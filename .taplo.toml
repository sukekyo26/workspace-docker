# taplo configuration
# https://taplo.tamasfe.dev/configuration/file.html

[[rule]]
include = ["workspace.toml"]
schema = { path = "./schemas/workspace.schema.json" }

[[rule]]
include = ["plugins/*.toml"]
schema = { path = "./schemas/plugin.schema.json" }
