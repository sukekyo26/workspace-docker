[metadata]
name = "Rust"
description = "Rust toolchain via rustup (cargo, clippy, rustfmt)"
default = false

[install]
requires_root = false
user_dirs = ["/home/${USERNAME}/.cargo", "/home/${USERNAME}/.rustup"]
dockerfile = '''
# Install Rust toolchain via rustup
RUN curl -fsSL --proto '=https' --tlsv1.2 https://sh.rustup.rs -o /tmp/rustup-init.sh && \
    echo "{{INSTALL_SCRIPT_SHA256}}  /tmp/rustup-init.sh" | sha256sum -c - && \
    sh /tmp/rustup-init.sh -y --default-toolchain stable --component clippy --component rustfmt && \
    rm -f /tmp/rustup-init.sh && \
    echo '. "$HOME/.cargo/env"' >> ~/.bashrc
ENV PATH="/home/${USERNAME}/.cargo/bin:$PATH"
'''

volumes = ["/home/${USERNAME}/.cargo", "/home/${USERNAME}/.rustup"]

[version]
strategy = "latest"
install_script_sha256 = "17247e4bcacf6027ec2e11c79a72c494c9af69ac8d1abcc1b271fa4375a106c2"
