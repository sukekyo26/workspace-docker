# Base apt packages for workspace-docker
# One package per line. Empty lines and lines starting with # are ignored.
# These packages are always installed in the development container.
#
# To add project-specific packages, use workspace.toml:
#   [apt]
#   extra_packages = ["your-package"]

# === Core tools ===
curl
wget
ca-certificates
gnupg
openssh-client
git
make
sudo

# === Editors ===
vim
nano

# === Archive / compression ===
zip
unzip
tar
gzip
bzip2
xz-utils

# === Utilities ===
tree
jq
bc
less
rsync
procps
iproute2
dnsutils
iputils-ping
net-tools
shellcheck
uuid-runtime
file
patch
gettext-base

# === Python (required by workspace-docker tooling) ===
python3
python3-pip
python3-venv

# === Locale / system ===
bash-completion
locales
